#include<stdio.h>
#include<pthread.h>

void *hello(){
  while(1){
    printf("hello!\n");
    sleep(1);
  }
}

int main(){
  pthread_t thread_id;
  int status;

  status = pthread_create(&thread_id, NULL, hello, (void *)NULL);
  pthread_detach(thread_id);
  sleep(3);

  printf("bye!\n");
}
