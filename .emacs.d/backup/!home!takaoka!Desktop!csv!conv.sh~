#!/bin/bash

DIR1="belonglist160201-converted"
DIR2="belonglist160201"

cat names.txt | while read line; do
    name1=$(echo "$line" | cut -d ' ' -f 2)
    name2=$(echo "$line" | cut -d ' ' -f 1)
    cp "$DIR1/$name1" "$DIR2/$name2"
done

