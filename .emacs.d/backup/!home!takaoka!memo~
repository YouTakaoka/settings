[videoroom]
2987 if(sendto(participant->udp_sock, buf, len, 0, (struct sockaddr*)&rtp_forward->serv_addr, sizeof(rtp_forward->serv_addr)) < 0) { 

2898 void janus_videoroom_incoming_rtp(janus_plugin_session *handle, int video, char *buf, int len) {

3005 janus_recorder_save_frame(video ? participant->vrc : participant->arc, buf, len);

3091 void janus_videoroom_incoming_rtcp(janus_plugin_session *handle, int video, char *buf, int len) {

3137 void janus_videoroom_incoming_data(janus_plugin_session *handle, char *buf, int len) {

3308 janus_videoroom_rtp_relay_packet


[echotest]
sendtoなし
no definition of janus_recorder_save_frame

675                        gateway->relay_rtp(handle, video, buf, len);

[janus_gateway/record.c]
199　int janus_recorder_save_frame(janus_recorder *recorder, char *buffer, uint length) { 

[piping]
Execute Janus in janus/bin/ -> It worked!!!
