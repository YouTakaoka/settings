#include<opencv2/opencv.hpp>

#define DATA_SIZE 500000
using namespace cv;

int main(){
  char* data = malloc(DATA_SIZE);

  FILE* file = fopen("out.bmp","r");
  int len = fread(data, 1, DATA_SIZE, file);

  // Create matrix object
  int raws = 96;
  int cols = 128;
  int type = CV_8UC1;
  size_t step = ;
  Mat mat(raws, cols, type, data, step);

  fclose(file);
  free(data);
  
  return 0;
}
