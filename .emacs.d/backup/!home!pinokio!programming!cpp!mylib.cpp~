#include<iostream>
#include<cstdio>
#include<algorithm>

using namespace std;

/*
*/


int pow(int x, int n){
  if(n == 0) return 1;
  else return x * pow(x, n - 1);
}

bool bit(int b, int i){
  return (b >> i) % 2;
}
